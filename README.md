# Resume Builder (Pandoc)

## Quick start
```
brew install pandoc
make all
```

Outputs:
- `output/Joe-Flack-Resume.html`
- `output/Joe-Flack-Resume.docx`

Edit content in `content/` and profile data in `data/meta.yml`.
